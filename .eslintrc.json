{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "semi": ["error", "never"],
    "no-unused-vars": [
      "error",
      { "destructuredArrayIgnorePattern": "^_", "argsIgnorePattern": "^_" }
    ]
  }
}
